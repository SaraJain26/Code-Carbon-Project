# Experiments

Use this directory for research experiments that explore new analyses, prediction features, model calibration ideas, or alternative source-code representations.

Experimental code should not become a runtime dependency of the static analysis package unless it is promoted into `src`.
