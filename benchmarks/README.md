# Benchmarks

This directory is reserved for benchmark manifests, benchmark harnesses, and benchmark result metadata.

Do not move the existing source examples from `examples/benchmarks`; those remain small analysis fixtures.
